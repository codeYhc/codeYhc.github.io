import{_ as a,c as s,o as e,a1 as t}from"./chunks/framework.CQAhCBne.js";const k=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"test.md","filePath":"test.md"}'),o={name:"test.md"},p=t(`<h2 id="安装-patch-package" tabindex="-1"><strong>安装 patch-package</strong> <a class="header-anchor" href="#安装-patch-package" aria-label="Permalink to &quot;**安装 patch-package**&quot;">​</a></h2><p>首先，我们需要安装 <code>patch-package</code> 和 <code>postinstall-postinstall</code>，它们将帮助我们生成和应用补丁。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>npm i patch-package postinstall-postinstall --save-dev</span></span></code></pre></div><h2 id="配置-package-json" tabindex="-1"><strong>配置 package.json</strong> <a class="header-anchor" href="#配置-package-json" aria-label="Permalink to &quot;**配置 package.json**&quot;">​</a></h2><p>在 <code>package.json</code> 中添加 <code>postinstall</code> 脚本，以便在每次依赖安装后自动应用补丁。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{</span></span>
<span class="line"><span>  &quot;scripts&quot;: {</span></span>
<span class="line"><span>    &quot;postinstall&quot;: &quot;patch-package&quot;</span></span>
<span class="line"><span>  }</span></span>
<span class="line"><span>}</span></span></code></pre></div><h2 id="创建补丁" tabindex="-1"><strong>创建补丁</strong> <a class="header-anchor" href="#创建补丁" aria-label="Permalink to &quot;**创建补丁**&quot;">​</a></h2><p>假设我们需要修改一个依赖包 <code>some-package</code>。我们可以按照以下步骤创建补丁：</p><ol><li><strong>修改依赖包</strong></li></ol><p>在 <code>node_modules/some-package</code> 中找到需要修改的文件，并进行必要的修改。例如，我们要修改 <code>index.js</code> 文件。</p><ol><li><strong>生成补丁</strong></li></ol><p>运行以下命令生成补丁文件：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>patch-package some-package</span></span></code></pre></div><p>这将在项目根目录下生成一个 <code>patches</code> 文件夹，其中包含生成的补丁文件，例如 <code>patches/some-package+1.0.0.patch</code>。</p><h2 id="确保补丁文件被添加到版本控制系统" tabindex="-1"><strong>确保补丁文件被添加到版本控制系统</strong> <a class="header-anchor" href="#确保补丁文件被添加到版本控制系统" aria-label="Permalink to &quot;**确保补丁文件被添加到版本控制系统**&quot;">​</a></h2><p>确保补丁文件被添加到你的版本控制系统（例如 Git）。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>git add patches</span></span>
<span class="line"><span>git commit -m &quot;Add patch for some-package&quot;</span></span></code></pre></div><h2 id="自动应用补丁" tabindex="-1"><strong>自动应用补丁</strong> <a class="header-anchor" href="#自动应用补丁" aria-label="Permalink to &quot;**自动应用补丁**&quot;">​</a></h2><p>每次运行 <code>pnpm install</code> 时，<code>postinstall</code> 脚本会自动运行 <code>patch-package</code>，从而应用补丁。</p>`,19),n=[p];function c(i,l,d,h,r,g){return e(),s("div",null,n)}const m=a(o,[["render",c]]);export{k as __pageData,m as default};
